//
// Created by 薛新岗 on 2024/9/10.
//


int main(){
//    // 使用单例模式获取配置实例
//    loadConfig& config = loadConfig::getInstance("../config/configs.yaml");
//    for (const auto& typeNode : config.getTypesConfig()){
//        std::string typeName = typeNode.as<std::string>();
//        if (typeName == "int") {
//            types.push_back(0); // 使用默认值
//        } else if (typeName == "double") {
//            types.push_back(0.0); // 使用默认值
//        } else if (typeName == "std::string") {
//            types.push_back(std::string{}); // 使用默认值
//        } else {
//            std::cerr << "Unknown type: " << typeName << std::endl;
//        }
//    }
//    std::cout << config.getTypesConfig() << std::endl;
//    config.printConfig();
//    return 0;
}